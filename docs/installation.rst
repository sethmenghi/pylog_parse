============
Installation
============

At the command line::

    $ easy_install pylog_parse

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pylog_parse
    $ pip install pylog_parse
